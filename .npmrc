; typescript@7 is ahead of @typescript-eslint's declared peer range (>=4.8.4 <6.1.0),
; so npm's strict peer resolution fails. Keep installs consistent everywhere
; (local, CI, Docker) until typescript-eslint publishes TS 7 peer support.
legacy-peer-deps=true
